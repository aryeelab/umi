language: python
python:
  - "2.6"
  - "2.7"

# command to install dependencies
install: "python setup.py install"

# command to run tests
script: cd test && nosetests --verbosity=3 -s

# Disable caching
cache: false